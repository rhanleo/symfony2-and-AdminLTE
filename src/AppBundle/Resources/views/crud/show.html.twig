{% extends '::base.html.twig' %}

    {% block body -%}
    <div class='container'>
        <h1>crud</h1>

        <table class="record_properties">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ entity.title }}</td>
                </tr>
                <tr>
                    <th>Author</th>
                    <td>{{ entity.author }}</td>
                </tr>
            </tbody>
        </table>

            <ul class="record_actions">
        <li>
            <a href="{{ path('crud') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('crud_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
    {{entity.id}}
    </div>
    {% endblock %}
